// ESLint configuration disabled for build compatibility
module.exports = {
  root: true,
  env: {
    browser: true,
    es2022: true,
    node: true,
  },
  extends: [],
  rules: {},
  ignorePatterns: [
    '**/*', // Ignore all files for now
  ],
};